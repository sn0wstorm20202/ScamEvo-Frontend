# Backend base URL (FastAPI)
NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:8000

# Optional: set a fixed request id from the UI (otherwise it auto-generates one per request)
# NEXT_PUBLIC_DEFAULT_REQUEST_ID=
